fastapi>=0.115.0
uvicorn>=0.30.0
pydantic[email]>=2.0
python-dotenv>=1.0
lightgbm>=4.0.0
google-auth
psycopg2-binary

httpx>=0.27.0
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1

# ── Scraper ───────────────────────────────────────────────
feedparser==6.0.11          # RSS parsing for businessnews.com.tn
beautifulsoup4==4.12.3      # HTML parsing for article bodies
httpx==0.27.0               # Async-capable HTTP client
lxml==5.2.1                 # Fast HTML/XML parser (bs4 backend)
playwright==1.44.0          # Headless browser for jawharafm (403 bypass)
                            # After install: playwright install chromium
 
# ── Scheduler ────────────────────────────────────────────
APScheduler==3.10.4         # Background job scheduler
 

apscheduler
apscheduler
